spring:
  datasource:
    hikari:
      schema: public
  jpa:
    properties:
      hibernate:
        default_schema: public
  liquibase:
    default-schema: public
    liquibase-schema: public
account:
  security:
    admin-user-ids: "12345"
  number:
    min-free: 10
    batch-size: 5
    max-free: 25
logging:
  level:
    root: info
    liquibase: info